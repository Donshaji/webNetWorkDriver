<Window x:Class="webNetWorkDriver.View.MetaDataViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:webNetWorkDriver.View"
        mc:Ignorable="d"
        Style="{StaticResource DarkWindow}"
        Title="MetaData Viewer" Height="500" Width="720"
        MinWidth="400" MinHeight="300"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="📋" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock Text="Page Metadata"
                       Style="{StaticResource SectionHeader}"
                       Margin="0"/>
        </StackPanel>

        <!-- MetaData Content -->
        <TextBox Grid.Row="1"
                 Text="{Binding MetaDataContent, Mode=OneWay}"
                 Style="{StaticResource CodeBox}"/>
    </Grid>
</Window>
